#include "main.h"
#include <string.h>

/**
 * _strcmp - Entry point
 * @s1: pointer to destination
 * @s2: pointer to source
 * Return: number
*/

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}

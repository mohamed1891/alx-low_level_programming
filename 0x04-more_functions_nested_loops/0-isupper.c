#include "main.h"
#include <ctype.h>
#include <stdio.h>

/**
 * _isupper - Entry point
 * @c: character to classify.
 * Return: 1 if c is uppercase or otherwise 0.
 *
 */

int _isupper(int c)
{
if isupper(c)
return (1);
else
return (0);
}

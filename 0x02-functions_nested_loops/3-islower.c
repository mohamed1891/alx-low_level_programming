#include "main.h"
#include <ctype.h>
#include <stdio.h>

/**
 * _islower - Entry point
 * @c: character to classify.
 * Return: 1 if c is lowercase or otherwise 0.
 *
 */

int _islower(int c)
{
if islower(c)
return (1);
else
return (0);
}
